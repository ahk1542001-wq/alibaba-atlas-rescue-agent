[pytest]
markers =
    mockdata: G7 victor mock-data pass — skips gracefully while the owner-supplied fixture carries placeholders
    live_atlas: requires the live Atlas Sandbox CLI (atlas-flight) on PATH — skips with a visible reason when it is absent (e.g. GitHub CI), since these tools perform a real provider search with no mock fallback by design
